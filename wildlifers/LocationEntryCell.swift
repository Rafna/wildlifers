import UIKit

class LocationEntryCell : UITableViewCell, DataEntryTableViewCell {
    var type = DataEntryCellType.Location
    
    func configure(configuration: LocationConfiguration) {
        
    }
}
